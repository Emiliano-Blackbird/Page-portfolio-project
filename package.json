{
  "name": "ejercicio2",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
      "deploy": "npm run build && gh-pages -d dist",
    "copy-dist-to-docs": "rm -rf docs && cp -r dist docs"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.1.0"
  },
  "dependencies": {
    "sass": "^1.85.0"
  }
}
